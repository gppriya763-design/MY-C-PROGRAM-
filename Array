#include <stdio.h>

int main() {
    int arr[100],n=3;
    for(int i = 0; i < 3; i++) {
        scanf("%d", &arr[i]);
    }

    printf("\nReversed array:\n");
    for(int i = n - 1; i >= 0; i--) {
        printf("%d ", arr[i]);
    }

    return 0;
}

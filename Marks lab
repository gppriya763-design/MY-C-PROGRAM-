#include <stdio.h>
int main() {
    int marks;
    printf ("enter the marks");
    scanf("%d",&marks);
    if (marks>=90)
    printf ("grad A");
  else if ( marks>=75)
    printf ("grad B");
    else if(marks>=60)
    printf ("grad c");
    else if(marks>=50)
    printf ("grad D");
    else  
    printf ("grad F");
}
    
    
